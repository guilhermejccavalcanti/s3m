name: Cron Test

on:
  schedule:
    - cron: '* * * * 5'

defaults:
  run:
    shell: bash

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ windows-latest, ubuntu-latest, macos-latest ]
        include:
          - os: windows-latest
            dependencies: .\;dependencies/*
          - os: ubuntu-latest
            dependencies: .:dependencies/*
          - os: macos-latest
            dependencies: .:dependencies/*

    steps:
    - uses: actions/checkout@v2

    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build
      run: |
        ./gradlew clean build -x test --console=plain
        cp build/libs/jFSTMerge-all.jar ${HOME}/s3m.jar

    - name: Configure git integration with S3M
      run: |
        bash testfiles/shelltests/config/gitconfiguration.sh
        git config --global user.name "Guilherme Cavalcanti"
        git config --global user.email "gjcc@cin.ufpe.br"

    - name: Run cron tests
      run: |
        cd testfiles/shelltests
        javac -cp ${{ matrix.dependencies }} s3mCronTests.java
        java -cp ${{ matrix.dependencies }} org.junit.runner.JUnitCore s3mCronTests