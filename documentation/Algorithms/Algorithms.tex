\documentclass[a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[boxruled, linesnumbered]{algorithm2e}
\usepackage{geometry}
\usepackage{amsmath}

\usepackage{subfiles}

\geometry{a4paper, margin=1in}

\SetKwFunction{ac}{addChild}
\SetKwFunction{rn}{removeNode}
\SetKwFunction{ttt}{treeToText}
\SetKwFunction{ec}{extractConflicts}

\SetKwFunction{find}{find}
\SetKwFunction{findFirst}{findFirst}

\SetKwFunction{tm}{textualMerge}
\SetKwFunction{conflict}{conflict}

\SetKw{True}{true}
\SetKw{False}{false}
\SetKw{Null}{null}
\SetKw{Break}{break}

\begin{document}
    \section{Early Concepts}

    \begin{itemize}
        \item \textbf{Nodes added by left/right}:
        
        $A_L \leftarrow \{l \in L \mid (\lnot \exists b \in B)(l.id = b.id)\}$

        $A_R \leftarrow \{r \in R \mid (\lnot \exists b \in B)(r.id = b.id)\}$

        \item \textbf{Nodes deleted by left/right}:
        
        $D_L \leftarrow \{b \in B \mid (\lnot \exists l \in L)(b.id = l.id)\}$

        $D_R \leftarrow \{b \in B \mid (\lnot \exists r \in R)(b.id = r.id)\}$

        \item \textbf{Nodes edited by left/right}:
        
        $E_L \leftarrow \{l \in L \mid (\exists b \in B)(\exists r \in R)(l.id = b.id = r.id \land l.body \neq b.body \land b.body = r.body)\}$

        $E_R \leftarrow \{r \in R \mid (\exists b \in B)(\exists l \in L)(r.id = b.id = l.id \land r.body \neq b.body \land b.body = l.body)\}$
    \end{itemize}

    \subfile{Merge/Merge.tex}

    \section{Handlers}

    \subfile{Handlers/Renaming/Renaming}
    \subfile{Handlers/InitializationBlocks/InitializationBlocks}
    \subfile{Handlers/MultipleInitializationBlocks/MultipleInitializationBlocks}
    \subfile{Handlers/TypeAmbiguityError/TypeAmbiguityError}
    \subfile{Handlers/NewElementReferencingEditedOne/NewElementReferencingEditedOne}
    \subfile{Handlers/Deletions/Deletions}
\end{document}